queue_rules:
  - name: default
    merge_conditions:
      - check-success=gate

pull_request_rules:
  - name: queue it
    conditions:
      - label=queue-me
    actions:
      queue:
        name: default
